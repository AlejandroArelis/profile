import { environment as defaultEnvironment } from './environment.defaults';

export const environment = {
  apiUrl: "http://127.0.0.1:8000",
  MSAL_REDIRECT_URI: "https://alejandroarelis.github.io/profile/"
};
